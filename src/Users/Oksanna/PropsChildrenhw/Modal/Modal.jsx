// import React from 'react'

export default function Modal({toggleModal, children }) {
    return (
        <div>
            <div>
                {children}
            </div>
        </div>
    )
}
